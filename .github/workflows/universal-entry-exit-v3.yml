name: Universal entry exit optimisation v3

on:
  pull_request:
    branches: [main]
    paths:
      - research/universal_entry_exit_20260721/**
      - .github/workflows/universal-entry-exit-v3.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  optimise-universe:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install --quiet --upgrade pip pandas numpy yfinance
      - name: Compile and validate configuration
        run: |
          cat research/universal_entry_exit_20260721/optimize_universal_entry_exit.py.part?? > /tmp/optimize_universal_entry_exit.py
          python -m py_compile /tmp/optimize_universal_entry_exit.py
          python -m py_compile research/universal_entry_exit_20260721/optimize_universal_entry_exit_runner.py
          python -m json.tool research/universal_entry_exit_20260721/universe_v3.json >/dev/null
      - name: Run universal optimiser
        shell: bash
        run: |
          mkdir -p research_outputs/universal_entry_exit_v3_20260721
          set +e
          PYTHONWARNINGS=ignore python research/universal_entry_exit_20260721/optimize_universal_entry_exit_runner.py > research_outputs/universal_entry_exit_v3_20260721/optimizer.log 2>&1
          code=$?
          tail -n 240 research_outputs/universal_entry_exit_v3_20260721/optimizer.log
          exit $code
      - name: Show summary
        if: always()
        run: |
          test -f research_outputs/universal_entry_exit_v3_20260721/summary.md && cat research_outputs/universal_entry_exit_v3_20260721/summary.md || true
      - name: Upload full audit artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: universal-entry-exit-v3-20260721
          path: research_outputs/universal_entry_exit_v3_20260721
          if-no-files-found: warn
          retention-days: 14
